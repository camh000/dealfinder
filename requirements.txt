# Web server
flask==3.0.3
gunicorn==23.0.0

# Database (1.1.14+: builds against MariaDB Connector/C 3.4 shipped by
# current Debian slim bases; 1.1.10's C extension fails to compile there)
mariadb==1.1.14

# Utilities
python-dotenv==1.0.1
requests==2.32.3
beautifulsoup4==4.12.3

# HTTP client with browser TLS fingerprinting (primary fetcher).
# >=0.8 fixes session memory leaks present in 0.7.x (sessions are also now
# explicitly closed on reset — see EbayScraper._close_direct_session).
curl-cffi>=0.9,<0.14
